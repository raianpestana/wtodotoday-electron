/* | - Imports - | */
/* - @mui - */
/* styles */ import { createTheme } from '@mui/material/styles'

/* | - Default Theme - | */
/* - DefaultTheme - */
export const DefaultTheme = createTheme({
  palette: {
    mode: 'dark',
    error: {
      main: '#c76060'
    }
  }
})
